export const is_visible_cilent_review = false;

// Admin navigation visibility. Routes and page implementations remain available.
export const is_visible_admin_gallery = false;
export const is_visible_admin_reviews = false;
